syntax = "proto3";

option go_package = "github.com/SaiNageswarS/agent-boot/schema";

package agent;

service Agent {
    rpc Execute(GenerateAnswerRequest) returns (stream AgentStreamChunk) {}
}

message GenerateAnswerRequest {
    string question = 1;
    string sessionId = 2;   // For including previous messages in agent inference.
    int32 maxIterations = 3;
    map<string, string> metadata = 4;
}

message AgentStreamChunk {
    oneof chunk_type {
        ProgressUpdateChunk      progressUpdateChunk = 1;
        ToolResultChunk          toolResultChunk = 3;
        AnswerChunk              answer = 4;
        StreamComplete           complete = 5;
        StreamError              error = 6;
    }
}

// Progress / metadata updates
enum Stage {
    tool_execution_starting = 0;
    tool_execution_failed = 1;
    tool_execution_completed = 2;
    answer_generation_starting = 3;
    answer_generation_failed = 4;
    answer_generation_completed = 5;
}

message ProgressUpdateChunk {
    Stage stage = 1;
    int64 timestamp = 2;
    string message = 3;
    int32 estimatedSteps = 5;
}

// Raw result chunk of a single tool.
// A single tool can emit multiple ToolExecutionResultChunk.
message ToolResultChunk {
    repeated string sentences = 1;
    string attribution = 2;
    string title = 3;
    map<string, string> metadata = 4;
    string toolName = 5;
    string error = 6;
    string id = 7;
}

// Final Answer Chunk
message AnswerChunk {
    string content = 1;
}

// End of Streaming.
message StreamComplete {
    string final_status = 1;
    string answer = 2;
    int32 tokenUsed = 3;
    int64 processingTime = 4;
    map<string, string> metadata = 5;
    repeated string toolsUsed = 6;
}

// Error chunk
message StreamError {
    string error_message = 1;
    string error_code = 2;
}